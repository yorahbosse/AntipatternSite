{{>navbar}}
<h4>Antipadrões</h4>

{{#each antipatterns}}
    
    <div class="card">
        <div class="card-body">
                <div class="card">
                    <div class="card-body">
                        <h4>ID: {{dataValues.ID}}</h4>
                        <h4>Sugestion_Teacher: {{dataValues.Sugestion_Teacher}}</h4>
                        <h4>Sugestion_Std: {{dataValues.Sugestion_Std}}</h4>
                        <h4>Problem: {{dataValues.Problem}}</h4>
                        <h4>isAntipattern: {{dataValues.isAntipattern}}</h4>
                        <h4>UserID: {{dataValues.UserID}}</h4>
                        <form action="/Antipattern/view" method="post">
                            <input type="hidden" name="ID" value="{{dataValues.ID}}">
                            <button type="submit" class="btn btn-info">Ver mais</button>
                        </form>
                    </div>
                </div>
        </div>
    </div>
    {{else}}
{{/each}}
